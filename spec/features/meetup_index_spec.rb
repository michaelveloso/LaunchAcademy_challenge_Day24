require 'spec_helper'

# As a user
# I want to view a list of all available meetups
# So that I can get together with people with similar interests
#
# Acceptance criteria:
# [ ] The user can see all meetups in alphabetical order
# [ ] Each meetup is a link to its show page

feature "User can see all meetups and click on them for details" do
  scenario "User can see all meetups" do
  end

  scenario "Meetups are in alphabetical order" do
  end

  scenario "Meetups are links to their show pages" do
  end
end
